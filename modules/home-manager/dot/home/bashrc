################################################################################
# Bashrc                                                                       #
################################################################################


if [ -s "${HOME}/.profile" ]; then
    . "${HOME}/.profile"
fi

if [ -s "/etc/bashrc" ]; then
    . "/etc/bashrc"
fi
